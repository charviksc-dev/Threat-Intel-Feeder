#!/usr/bin/expect -f
set timeout 600

spawn ssh -o StrictHostKeyChecking=no innopay@10.81.20.148 "cd ~/neev-tip && docker compose build ui && docker compose up -d ui"
expect {
    "assword:" {
        send "Innopay@123\r"
        exp_continue
    }
    eof
}
