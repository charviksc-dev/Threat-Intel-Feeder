#!/usr/bin/expect -f
set timeout 10

spawn ssh -o StrictHostKeyChecking=no innopay@10.81.20.148 "curl -s http://localhost:8000/api/v1/health && echo '' && curl -s http://localhost:8000/api/v1/stats | head -c 500"
expect {
    "assword:" {
        send "Innopay@123\r"
        exp_continue
    }
    eof
}
