#!/usr/bin/expect -f
set timeout 600

spawn ssh -o StrictHostKeyChecking=no innopay@10.81.20.148 "cd ~/neev-tip && docker compose build api && docker compose up -d api"
expect {
    "assword:" {
        send "Innopay@123\r"
        exp_continue
    }
    eof
}
